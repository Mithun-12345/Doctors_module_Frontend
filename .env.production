# Production — set to your VM's base URL (no trailing slash, no /api suffix)
# Example: VITE_API_URL=http://192.168.1.100
# If nginx proxies /api on the same domain, you can leave this empty
VITE_API_URL=
